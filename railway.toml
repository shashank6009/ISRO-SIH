[build]
builder = "nixpacks"

[deploy]
startCommand = "streamlit run src/genesis_ai/app/competition_dashboard.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
